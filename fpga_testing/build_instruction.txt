# install verilator as a package:
$ sudo apt-get install verilator


# Fusoc installer
$ sudo pip3 install –upgrade fusesoc

sudo apt install gtkwave

#Here we can change .v file name 
verilator --cc circuit.v

#Here we can change .v file name 
verilator -Wall --cc circuit.v --exe testbench.cpp (or) verilator -Wall -- trace --cc circuit.v --exe testbench.cpp

make -C obj_dir -f Vcircuit.mk Vcircuit

./obj_dir/Vcircuit
